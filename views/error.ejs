<%- include('./partial/header') %>

<div class="w-full h-screen flex flex-col justify-center items-center bg-zinc-100">
    <h1 class="text-4xl font-bold text-red-500 mb-4"><%= error %></h1>
    <% if (message && message.length > 0) { %>
        <p class="text-gray-600"><%= message %></p>
    <% } %>
    <a href="/" class="mt-8 px-6 py-2 bg-blue-500 text-white rounded-full hover:bg-blue-600">
        Go Home
    </a>
</div>

<%- include('./partial/footer') %>